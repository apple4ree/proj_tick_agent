# ==============================================
# Smoke 프로필 — 배선 확인용(quick wiring check)
# 최종 품질 게이트 용도가 아님
# ==============================================

app:
  env: smoke

generation:
  backend: template
  mode: mock
  auto_approve: true

backtest:
  compute_attribution: false

backtest_worker:
  latencies_ms: [0.0, 100.0]

workers:
  once: true
